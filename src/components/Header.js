import React from 'react';
import logo from '..//assets/logo.png'

const Header = () => {
    return (
        <div>
            <img src={logo} alt="" srcset="" />
        </div>
    );
};

export default Header;